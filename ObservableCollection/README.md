# ObservableCollection

Index only — the documentation lives with each package.

| Project | Package | Purpose |
|---|---|---|
| [MintPlayer.ObservableCollection](MintPlayer.ObservableCollection/README.md) | `MintPlayer.ObservableCollection` | A replacement `ObservableCollection<T>`: `AddRange` raising `CollectionChanged` once, item-property monitoring, WPF support, and updates from any thread. |
| [MintPlayer.ObservableCollection.Extensions](MintPlayer.ObservableCollection.Extensions/README.md) | `MintPlayer.ObservableCollection.Extensions` | Bulk, bounded and distinct operations for the **built-in** `ObservableCollection<T>` — usable on its own. |

`MintPlayer.ObservableCollection.Avalonia`, `.Avalonia.Desktop` and `.Test` are sample and test
projects; they are not published.
