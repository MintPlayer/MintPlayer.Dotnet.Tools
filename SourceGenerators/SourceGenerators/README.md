# Dependency-injection generators

Index only — the documentation lives with the package.

| Project | Package | Purpose |
|---|---|---|
| [MintPlayer.SourceGenerators](MintPlayer.SourceGenerators/README.md) | `MintPlayer.SourceGenerators` | Service registration (`[Register]`), constructor-free injection (`[Inject]`), configuration binding, and the interface-implementation analyzer. **Start here.** |
| MintPlayer.SourceGenerators.Attributes | `MintPlayer.SourceGenerators.Attributes` | The attributes you decorate with. Referenced automatically by the generator package. |
