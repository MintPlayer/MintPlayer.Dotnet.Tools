tls:
  options:
    mtls:
      minVersion: VersionTLS12
      maxVersion: VersionTLS12
      alpnProtocols:
        - http/1.1
      clientAuth:
        clientAuthType: RequestClientCert
        caFiles:
          - /etc/traefik/dynamic/belgian-eid-cas.pem
