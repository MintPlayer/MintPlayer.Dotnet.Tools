{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Source Generator",
      "type": "coreclr",
      "request": "attach",
      "preLaunchTask": "BuildValueComparerDebugging",
      "processId": "${command:extension.commandvariable.file.content,./pid.txt}"
    }
  ]
}
